# Demo: NYC taxi feature store + MLflow training notebook + inference endpoint
# pandas, pyarrow, scikit-learn are present in the saturn base image.
feast>=0.63
mlflow>=3.0
fastapi>=0.115
uvicorn>=0.32
